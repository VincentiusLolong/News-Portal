# Portal Berita 

## Development

cara menjalankan aplikasi (visual studio code)

### API
> cd ./app
> use `npm install`
> npm server:start


cara menjalankan aplikasi (Browser [XAMPP] )
### frontend
> open folder project, go to ./app folder
> use `npm install`
> npm start
> you can access dashboard admin ini `localhost:3000/account`, username "vincent@gmail.com" and password "admin1234"

isi folder :
#API [Berisi]
Server node.js (Rest API)

#APP [Berisi]
PHP berada di foder pages 
React.js, javascript

app.php (Router Front-end PHP) : untuk routing URL di website ini
